apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: studyhall
  namespace: prod
spec:
  secretName: studyhall-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - 'studyhall.org'
  - '*.studyhall.org'